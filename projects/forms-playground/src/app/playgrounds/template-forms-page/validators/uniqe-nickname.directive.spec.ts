import { UniqeNicknameDirective } from './uniqe-nickname.directive';

describe('UniqeNicknameDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqeNicknameDirective();
    expect(directive).toBeTruthy();
  });
});
